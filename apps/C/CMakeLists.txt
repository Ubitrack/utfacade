SET(UTCWRAPPER_REQUIRED_DEPS utcore utdataflow utvision utfacade)
if(BUILD_UTCWRAPPER)
    ut_include_modules(${UTCWRAPPER_REQUIRED_DEPS})

	SET(CMAKE_SWIG_FLAGS "")

	SET_SOURCE_FILES_PROPERTIES(interface.i PROPERTIES CPLUSPLUS ON)
	SET_SOURCE_FILES_PROPERTIES(interface.i PROPERTIES SWIG_FLAGS "-ignoremissing")

	SWIG_ADD_MODULE(ubitrack_c cffi interface.i)
	SWIG_LINK_LIBRARIES(ubitrack_c ${UBITRACK_LINKER_LIBS} ${UTCWRAPPER_REQUIRED_DEPS})
endif(BUILD_UTCWRAPPER)

